import { View as TaroView, Text as TaroText, Button as TaroButton } from '@tarojs/components';

// Export Taro components directly to match the interface of primitives.tsx
export const View = TaroView;
export const Text = TaroText;
export const Button = TaroButton;
